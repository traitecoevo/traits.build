% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/test_functions.R
\name{util_allowed_characters}
\alias{util_allowed_characters}
\title{The characters allowed outside ASCII}
\usage{
util_allowed_characters()
}
\value{
A length-1 string containing every allowed non-ASCII character
}
\description{
Built from the \code{allowed_characters} section of the schema, which is the single
global definition of what is permitted — see
\code{inst/support/traits.build_schema.yml}. It is deliberately not configurable
per dataset: one database, one answer.
}
\details{
Letters come from \strong{ranges} rather than a hand-written list. Enumerating them
individually is what left 14 letters allowed in lower case but not upper, so
that \code{Ósvaldsson} was reported while \code{ósvaldsson} would not have been. Ranges
contribute only their letters, so a symbol that happens to share the range
has to be admitted deliberately under \code{symbols}.
}
\keyword{internal}
