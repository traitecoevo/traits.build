% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/setup.R
\name{build_export}
\alias{build_export}
\title{Write a built database to \code{export/data/curr}}
\usage{
build_export(
  database_name = "database",
  path = file.path("export", "data", "curr")
)
}
\arguments{
\item{database_name}{Name of the database, matching the one given to
\code{\link[=build_setup_pipeline]{build_setup_pipeline()}}. A pipeline set up with a \code{database_name} other
than the default must be exported with that same name.}

\item{path}{Directory to write into}
}
\value{
The path written, invisibly
}
\description{
The \code{targets} pipeline rebuilds the compilation but does not write the
exported \code{.rds}: that write is around a third of a one-dataset rebuild, and
it is a publishing step rather than part of checking a dataset. This writes
it, reading the compilation \code{targets::tar_make()} has already built.
}
\examples{
\dontrun{
build_setup_pipeline(method = "targets", database_name = "austraits")
targets::tar_make()
build_export("austraits")
}
}
\seealso{
\code{\link[=build_setup_pipeline]{build_setup_pipeline()}}
}
