% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/skills.R
\name{use_traits_build_skills}
\alias{use_traits_build_skills}
\title{Install traits.build Claude Code skills into a database repo}
\usage{
use_traits_build_skills(path = ".", overwrite = FALSE)
}
\arguments{
\item{path}{Path to a traits.build database repo. Defaults to the current
directory.}

\item{overwrite}{Overwrite a skill already installed at \code{path}. Default
\code{FALSE}: an already-installed skill is left untouched and reported as
skipped, rather than silently clobbered.}
}
\value{
Invisibly, a character vector of the skill directories written to
\verb{<path>/.claude/skills/}. Skipped skills are not included.
}
\description{
\code{traits.build} ships one or more Claude Code skills under \verb{inst/skills/}
(visible while developing the package itself via the symlinked
\verb{.claude/skills/}). \code{use_traits_build_skills()} copies them into a
\emph{database} repo -- \code{austraits.build}, \code{ausinvertraits.build}, \code{AusFizz}, or
any other repo built on \code{traits.build} -- so an agent working there can use
them.
}
