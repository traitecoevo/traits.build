% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{util_parse_collection_date}
\alias{util_parse_collection_date}
\title{Standardise a messy \code{collection_date} (or raw date) column}
\usage{
util_parse_collection_date(x, excel_origin = "1899-12-30")
}
\arguments{
\item{x}{A character, \code{Date}, or \code{POSIXct} vector of raw date values --
the whole column at once, not one value at a time, since resolving a
plain numeric date's order depends on seeing the rest of \code{x}}

\item{excel_origin}{Origin date for numeric Excel serial dates. The
default (\code{1899-12-30}) is correct for a workbook built on Windows Excel;
one built on old Mac Excel needs \code{1904-01-01} instead}
}
\value{
Character vector the same length as \code{x}. Values already valid, or
safely resolved to \code{yyyy}, \code{yyyy-mm} or \code{yyyy-mm-dd} (singly or as a
\code{/}-delimited range), are returned in that form; anything left ambiguous
or unrecognised is returned completely unchanged
}
\description{
Best-effort conversion of raw date values into the format
\code{\link[=dataset_test]{dataset_test()}}'s \code{collection_date}-parses check requires: a year
(\code{yyyy}), a year and month (\code{yyyy-mm}), or a full date (\code{yyyy-mm-dd}),
singly or as a \code{start/end} range. Meant to be called from a dataset's
\code{custom_R_code}, before \code{collection_date} is otherwise read in, e.g.:
}
\details{
\if{html}{\out{<div class="sourceCode">}}\preformatted{custom_R_code: 'data \%>\% mutate(Date = util_parse_collection_date(Date))'
}\if{html}{\out{</div>}}

\strong{What this deliberately will not touch}: a bare numeric date like
\code{01/02/2008}, seen \emph{in isolation}, is genuinely ambiguous -- day-first or
month-first? -- and guessing wrong silently corrupts the data worse than
not guessing at all. Two things narrow that down first, though: (1) the
value's own two leading numbers -- \code{10/23/2021} can only be month-first,
since no month is \code{23} -- and only when that alone doesn't settle it, (2)
the rest of the column, on the view that a single source normally writes
dates one way (e.g. \code{9/14/2020} elsewhere rules out month \code{14}, settling
an otherwise-ambiguous \code{8/11/2020}). Real data can still mix conventions
\emph{within} one column, though -- e.g. automated timestamps in month-first
order alongside hand-entered dates in day-first order -- so (1) is
checked per value, never overridden by (2), and if the column as a whole
disagrees with itself, (2) is not applied at all rather than picking a
side. Only once neither of those applies does a value come back
unchanged. Run \code{\link[=dataset_test]{dataset_test()}} again afterwards: whatever it still flags
under "Some date values are not parsing" needs a human decision, not a
better regex.

\strong{What it does handle}:
\itemize{
\item Values already valid are passed through untouched
\item A \emph{named} month removes the day/month ambiguity regardless of which
order it was written in, so \code{2-Sep-08}, \verb{September 2, 2008}, \verb{2 Sep 2008} and similar all resolve safely
\item A month name with no day (\verb{Sep 2008}) resolves to the \code{yyyy-mm}
resolution, not a fabricated day-1 date
\item A plain numeric \code{d/m/y}-shaped date, resolved per-value or from the
rest of the column as described above
\item \code{mm-yyyy} (month first, the reverse of the schema's \code{yyyy-mm}) --
unambiguous on its own, since a 4-digit year can't be mistaken for
anything else
\item A dot-separated \code{yy.mm.dd}/\code{yyyy.mm.dd}, confirmed (not assumed) by
checking the middle component is a plausible month and the last a
plausible day
\item Excel's numeric serial dates (e.g. \code{39692}), distinguished from a bare
4-digit year by digit count
\item A trailing time-of-day is dropped, both \code{"yyyy-mm-ddTHH:MM:SSZ"} (ISO
8601) and \code{"d/m/y HH:MM"} (e.g. a Google Forms timestamp column) --
\code{collection_date} has no way to record a time, only the date
\item A \code{start/end} range is split on \code{/} and each side parsed independently,
\emph{unless} the whole value already looks like a plain \code{d/m/y}-shaped date
(in which case \code{/} is a date separator, not a range separator, and the
rules above apply to the whole thing)
\item \code{Date}/\code{POSIXct} columns (already parsed by \code{read_csv()}'s own type
guessing before \code{custom_R_code} runs) are formatted directly, dropping
any time-of-day component
}
}
\examples{
util_parse_collection_date(c(
  "2-Sep-08", "September 2, 2008", "Sep 2008", "39692",
  "2008-09-02/2008-09-03", "01/02/2008", "2020", NA
))

# the whole column is used to resolve a plain numeric date's order:
# `9/14/2020` can only be month-first, so `8/11/2020` is read the same way
util_parse_collection_date(c("8/11/2020 10:09", "9/14/2020 12:06"))
}
